{
  "name": "olico-match",
  "version": "0.0.3",
  "description": "demand offer match logic and objects",
  "main": "index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "build": "mkdir ./build",
    "clean": "rm -rf ./build/*",
    "test": "mocha ./test/unit/",
    "test:e2e": "mocha test/e2e",
    "test:dev": "nodemon --watch ./test/ --watch ../src/ --exec npm test",
    "mongoup": "mongod --config=./test/e2e/default.conf"
  },
  "author": "alex@systemics.gr",
  "license": "ISC",
  "dependencies": {
    "mongodb": "3.2.3"
  },
  "devDependencies": {
    "mocha": "^6.1.2",
    "nodemon": "^1.18.11"
  }
}
